#!/usr/bin/env node

require('dotenv').config()
// const port = require('../config').port || 3000;
const port = process.env.PORT || 3000;
// const app = require('../app');
const app = require('../standalone-server');
app.listen(port, () => {});
